<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <link rel="shortcut icon" href="https://cdn-icons-png.flaticon.com/512/295/295128.png" type="image/x-icon">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        .forgot-password {
            padding: 80px 0;
        }
        .forgot-password__form {
            background: #ffffff;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .forgot-password__form h3 {
            margin-bottom: 30px;
            color: #333;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-group input {
            width: 100%;
            padding: 10px 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
        }
        .site-btn {
            width: 100%;
            padding: 12px;
            background: #333;
            color: #fff;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.3s;
        }
        .site-btn:hover {
            background: #e53637;
        }
        .forgot-password__form__social {
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
        }
        .forgot-password__form__social a {
            color: #e53637;
            text-decoration: none;
        }
        .forgot-password__form__social a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <section class="forgot-password">
        <div class="container">
            <div class="row">
                <div class="col-lg-6 offset-lg-3">
                    <div class="forgot-password__form">
                        <h3>Forgot Password</h3>
                        <form action="/forgot-password" method="POST">
                            <div class="form-group">
                                <input type="email" name="email" placeholder="Enter your email" required>
                            </div>
                            <button type="submit" class="site-btn">Send OTP</button>
                        </form>
                        <div class="forgot-password__form__social">
                            <p><a href="/signin">Back to Sign In</a></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</body>
</html>
